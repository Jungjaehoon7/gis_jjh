
# 삼각형 넓이 계산 함수 작성
# 사각형 넓이 계산 함수 작성
#
# +
#
# 입력값이 모두 양수인지 확인
# 아닐경우 에러 발생시키는
#
# decorator

x,y

def check_int(func):

    def decorated(w, h):

        if w>=0 and h>=0:

            return func(w,h)

        else:
             raise ValueError('Input must be positive value')
    return
        decorated





        print('x*y')

    return decorated

@check_int

def (detector):

    if x>0, y>0:

    else:
        print('error')

detector()

# 2.
#
# user 클래스 작성
# user 클래스 내 in_authenticated변수 작성
#
# user 객체를 넓이 함수 인자로 전달
#
# is_authenticated 변수를 확인하고,
# True가 아닐경우 error 발생
#
#


